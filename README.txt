The application now contains a `src/` directory that contains our application source.
You can try running the application just by opening the `index.html` file.

Try playing with it a little, change how the template looks, try to break it.

This is a minimal angular application.
