Currently $localStorage saves things under `ngStorage-` prefixed keys
in local storage. You can check this in google dev tools.

Use config function of your application to set the prefix to company name
given in configuration.